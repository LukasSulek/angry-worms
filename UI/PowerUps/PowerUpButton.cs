using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PowerUpButton : MonoBehaviour
{/*
    [SerializeField] private Button _powerUpButtonComponent;
    public Button PowerUpButtonComponent
    {
        get { return _powerUpButtonComponent; }
    }
    [SerializeField] private PowerUpSlider _powerUpSlider;

    
    void Start()
    {
        _powerUpButtonComponent.onClick.AddListener( delegate { PowerUpButtonTapped(); });
    }


    public void PowerUpButtonTapped()
    {
        if(_powerUpSlider.PowerUpInteractable == true)
        {
            Debug.Log("Power-Up");
            _powerUpSlider.PowerUpInteractable = false;
            _powerUpSlider.CurrentValue = 0;
            _powerUpSlider.WormCountSlider.value = 0;
        }
        else
        {
            Debug.Log("Not enough worms tapped!");
            return;
        }
    }






*/


}
